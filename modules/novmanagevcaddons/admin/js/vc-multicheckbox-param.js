!function($) {	$('body').on('click', '.input-checkbox', function () {		var lookbooks = '';		var temp_chk = 0;		$fstyle = $(this).parent().parent().parent();		var tmp_array = new Array();		$fstyle.find('.input-checkbox').each(function (index, checkbox) {			if ($(this).is(':checked')) {				var val = $(this).val();				tmp_array.push(val);			}		});		var checkboxs = '';		$.each(tmp_array,function (index, value) {			if (index != 0) {				checkboxs += ',';			}			checkboxs += value;		});		$fstyle.find('.nov-checkboxs-value').val(checkboxs);	});}(window.jQuery);!function($) {	$('.checkbox-lists').each(function(index, element){		$checkboxs = $(this);		var nov_checkboxs_value = $checkboxs.find('.nov-checkboxs-value').val();		if(nov_checkboxs_value != '')		{			var vals = nov_checkboxs_value.split(',');			$.each(vals, function(i, vl){				if(vl != '')				{					$checkboxs.find('.input-checkbox').each(function(input_index, input){						var input_value = $(input).val();						if(vl == input_value)							$(input).attr('checked', true);					});				}			})		}	});}(window.jQuery);